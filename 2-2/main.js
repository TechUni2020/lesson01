// Calculate the area of a circle with radius.
const r = process.argv[2];
// Use Math.pow(a, b); where a is the value and b is the exponent.
const area = 3.14 * Math.pow(r, 2);
console.log(`${area}`);
